import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '41722-BhaktaWachela',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
